[package]
name = "snapshot-probe"
version = "0.0.0"
edition = "2021"

[dependencies]
deno_resolver = { path = "DENO_ROOT/libs/resolver" }
deno_npm_installer = { path = "DENO_ROOT/libs/npm_installer" }

[workspace]
